export const types = `
    type Frequency {
        counter: String!
        period: String!
    }
`;

export const inputs = `
    input FrequencyInput {
        counter: String!
        period: String!
    }
`